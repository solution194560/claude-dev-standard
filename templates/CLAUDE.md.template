# {{프로젝트명}} 운영 가이드

최종 업데이트: {{YYYY-MM-DD}}

<!-- ─────────────────────────────────────────────────────────
  이 파일은 Claude Code가 매 대화마다 자동으로 읽는 프로젝트 지침입니다.
  {{ }} 부분을 내 프로젝트 값으로 바꾸세요. 특히 §0 프로젝트 프로필은
  서브에이전트 5종이 참조하므로 반드시 채워야 합니다.
  다 채운 뒤 이 주석 블록은 삭제하세요.
───────────────────────────────────────────────────────── -->

## 0) 프로젝트 프로필 (에이전트 참조용 — 필수 기재)

| 항목 | 값 |
|---|---|
| 개요 | {{프로젝트 개요 — 한두 문장}} |
| 실행 명령 | `{{실행 명령 — 예: .venv/bin/python main.py (Windows: .venv\Scripts\python.exe main.py)}}` |
| 테스트 명령 | `{{테스트 명령 — 예: .venv/bin/python -m pytest tests/ -v}}` |
| 주요 산출물 경로 | {{예: results/ 폴더의 *.json, *.md}} |
| 외부 점검 도구 | {{Codex 등 점검 CLI 실행 명령 — 없으면 "없음"}} |

### 위험 작업 목록 (에이전트 실행 금지 — 드라이런까지만)
<!-- 운영 시스템에 실제로 반영되는 명령을 모두 나열하세요.
     에이전트는 이 목록의 명령을 절대 실행하지 않고, 드라이런 출력까지만 확인합니다.

     ⚠️ 여기에 명령을 추가할 때마다 .claude/settings.json 의 "deny" 에도
     같은 명령을 추가하세요. 이 목록은 지시문(에이전트가 읽고 지키는 규칙)이고,
     deny 는 강제(도구 호출 자체를 차단)입니다. 둘이 어긋나면 지시문만 남습니다. -->
- {{예: post_comments.py --apply — 실제 댓글 게시}}
- {{예: deploy.sh (Windows: deploy.ps1) — 운영 배포}}
- 대화형 확인 프롬프트(`[y/N]`)에 `y` 응답하는 행위 일체

## 1) 작업 원칙 (무조건 준수)
- **소스 작성 전 구성/설계 먼저 제시(2~3안 비교) → 확인 → 구현**: 새 소스(스크립트/
  모듈/함수)를 만들 때는 반드시 먼저 **최선의 방안 2~3가지**를 구성(파일 배치·함수
  구조·동작 흐름)과 함께 제시하고, **각 안의 장단점을 정리**해 보여준 뒤, 사용자
  확인(방안 선택)을 받고 나서 실제 코드를 작성한다. 확인 없이 바로 구현하지 않는다.
  - 기존 코드 조회·분석(Read/Grep 등)은 확인 없이 진행 가능.
  - 새 파일 생성, 로직 추가/변경은 확인 후 진행.
  - 방안이 사실상 1개뿐이거나 사용자가 이미 방식을 구체적으로 지정한 경우는 예외 —
    그 경우 단일안 설계만 제시하고 확인받는다.

## 2) 변경 이력·세션 기록
작업 기록은 [CHANGELOG.md](CHANGELOG.md)에 분리 관리한다(CLAUDE.md는 매 턴 자동
로드되므로 얇게 유지 — 토큰 비용 절감). **새 작업을 완료하면 CLAUDE.md가 아니라
CHANGELOG.md 맨 위에 기록할 것.**

**진행 중** 작업의 체크포인트는 `SESSION.md`에 남긴다(중단 시 갱신: 하던 일·다음 할
일·확정된 결정·재개 명령 — 완료되면 비우고 CHANGELOG로). 세션 재개 시 SESSION.md 를
먼저 읽는다.

## 3) 실행
<!-- Windows 프로젝트라면 아래 두 블록의 bash 를 powershell 로 바꾸세요. -->
```bash
cd {{프로젝트 루트 경로}}
{{실행 명령}}
```

테스트
```bash
{{테스트 명령}}
```

## 4) 개발 프로세스 (서브에이전트 — .claude/agents/)
새 기능/개편은 5단계 게이트로 진행한다. **각 단계 산출물의 판정이 통과일 때만
다음 단계로 진행한다.**

| 단계 | 에이전트 | 산출물(게이트) |
|---|---|---|
| 1 계획 수립 | plan-writer | PLAN_<주제>.md |
| 2 계획 점검 | plan-reviewer | *_REVIEW.md — APPROVE/REVISE |
| 3 구현 | implementer | 코드 + CHANGELOG (Phase 단위) |
| 4 구현 검증 | impl-verifier | *_VERIFY_*.md — PASS/FAIL |
| 5 최종 테스트 | final-tester | *_FINAL_*.md — DONE/BLOCKED |

- 점검(2)·검증(4)은 §0 프로필의 "외부 점검 도구"가 설정된 경우 그 도구의 결과를
  판정 기준으로 삼는다(도구 실패 시 에이전트 직접 점검 폴백, 사유 명기).
  테스트 실행은 도구와 무관하게 에이전트가 항상 직접 수행.
- 모든 에이전트: §0 "위험 작업 목록"의 실쓰기 금지(드라이런까지).
- 단건 수정(버그픽스 등)은 경량 경로 가능: 설계 확인 → 구현 → 테스트 →
  실데이터 확인 → CHANGELOG 기록.
- 호출 예: "plan-reviewer 서브에이전트로 PLAN_<주제>.md 점검해줘"

## 5) 정책 요약
<!-- 프로젝트 고유의 도메인 규칙(판정 기준, 매핑 표 등)을 여기에 유지하세요.
     규칙이 코드(config)에 있다면 여기엔 요약만 두고 파일 경로를 링크하세요. -->
{{프로젝트 도메인 정책 — 없으면 이 섹션 삭제}}
