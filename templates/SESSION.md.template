# 세션 체크포인트 ({{YYYY-MM-DD HH:MM}})

<!-- ─────────────────────────────────────────────────────────
  진행 중인 작업만 적습니다. 완료된 작업은 CHANGELOG.md 로 옮기고 여기는 비웁니다.
  중단할 때 Claude 에게 "SESSION.md 에 체크포인트 남겨줘" 한 마디면 갱신됩니다.
  재개할 때는 "SESSION.md 읽고 이어서 진행해줘".
───────────────────────────────────────────────────────── -->

## 지금 하던 일
- {{예: PLAN_로그인.md Phase B 구현 중 — 모델 함수는 완성, API 연결부 미완}}

## 다음에 할 일 (순서대로)
<!-- 가장 중요한 항목. 다음 세션의 Claude 는 1번부터 시작합니다. -->
1. {{예: routes.py 에 로그인 엔드포인트 연결}}
2. {{예: pytest 실행 → 통과하면 impl-verifier 검증 → CHANGELOG 기록}}

## 지금까지의 판단/결정 (다시 설명 안 해도 되게)
<!-- 확정된 선택만. 없으면 다음 세션이 같은 논의를 반복합니다. -->
- {{예: 세션 저장은 A안(서버 사이드)으로 확정 — REVIEW 에서 B안 보안 결함 지적됨}}

## 재개할 때 첫 명령
- {{예: "SESSION.md 읽고 이어서 진행해줘"}}

<!-- 파일 경로·문서명은 정확히 적으세요. "아까 그 계획" 은 다음 세션이 못 찾습니다. -->
