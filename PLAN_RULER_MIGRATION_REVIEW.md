<!-- PLAN_RULER_MIGRATION 계획 점검 보고서 — 3차 점검(2차 REVISE 후 재점검, 2차 보고서 대체본) -->
# PLAN_RULER_MIGRATION_REVIEW — 계획 점검 보고서 (3차)

- **이 문서는 3차 점검(2차 REVISE 후 재점검) 보고서이며, 2차 점검 보고서를 대체한다.**
  1·2차 점검 결과·판정 요지는 [PLAN_RULER_MIGRATION_REVIEW_JUDGE.md](PLAN_RULER_MIGRATION_REVIEW_JUDGE.md)
  (2차까지 REVISE 확정)에 남아 있고, 2차 잔존 결함 2건([A] O3 키 문구 raw·[B] §3.5 --no-backup)은
  아래 "해소 여부 표" 에 기준으로 인용한다.
- 점검 대상: [PLAN_RULER_MIGRATION.md](PLAN_RULER_MIGRATION.md) **3차 개정본**
- 배경 문서: [PLAN_RULER.md](PLAN_RULER.md)(1차 테스트, FINAL DONE) ·
  [RULER_TEST_RESULT.md](RULER_TEST_RESULT.md)(시나리오 15/15) ·
  [PLAN_RULER_FINAL_2bc_JUDGE.md](PLAN_RULER_FINAL_2bc_JUDGE.md)(DONE 통과 조건부)
- 점검 일자: 2026-07-12
- 프로세스 위치: 2/5 계획 점검 (plan-reviewer) — 3차 재점검

## 점검 수행자
- **외부 점검 도구 사용 (기본 경로)**: CLAUDE.md §0 "외부 점검 도구" = `codex exec -m gpt-5.5 --skip-git-repo-check -C . --sandbox read-only -`.
  3차 재점검 맥락(2차 잔존 2건 해소 확인 + grep -F 실측 요구 + 플래그 2단 유보 수용성 판정 +
  잔존의 (A)/(B) 구분·조건부 APPROVE 검토, 무한 반려 금지 지침)을 지시문에 포함해 스크래치패드
  파일로 저장 후 stdin 파이프로 실행. 정상 완료.
  - codex `tokens used` (3차): **40,561**
  - 전체 실행 로그 원본 경로: `/Users/hyundeok/.claude/projects/-Users-hyundeok-cluade-claude-dev-standard/cea20373-f13b-4c4b-b6e4-3a90f86f70b7/tool-results/bgfjbmiv0.txt`
  - codex 최종 응답 원문은 이 보고서 맨 아래 **부록 A** 에 전문 첨부.
- **실행자(plan-reviewer) 직접 검증 병행**: §3.1 키 문구 4건을 CLAUDE.md 에 대해 `grep -F`
  **직접 실행**해 삭제 전 매치를 실측했고(아래 표), §3.5 apply 명령 플래그가 스크립트 요구와
  통일됐는지 원문 확인했다. codex 도 동일 grep -F 실측으로 같은 결론에 도달(교차 일치).

---

## 권고: **APPROVE 권고 (조건부 — 후속 처리 조건 2건 첨부)**

> 최종 판정은 plan-reviewer 가 아니라 **gate-judge** 가 확정한다(재판정 필요).
> 이 보고서는 증거 수집·권고이다.

외부 점검 도구(codex)의 3차 판정이 **APPROVE (조건부) 권고**이며, 실행자 grep -F 직접 실측도
이에 동의한다. 2차 잔존 결함 2건이 **전부 해소**됐고, **(A) 계획 문서로서의 반려 결함은
발견되지 않았다.** 남은 사항은 `--no-gitignore`·`--no-backup` 플래그 실재 확인처럼 Phase 1
구현·검증 첫 단계에서 자연히 확정될 (B) 세부뿐이며, 계획이 이미 [B-1] 캡처 절차와 미실재
폴백(플래그 제거 + toml 신뢰 + git diff 방어)을 명시하고 있어 통제된다. 1·2차에서 반려를
만들던 축(스크립트 플래그·헤더 사실·O3 설계·키 문구 raw 정합)이 모두 닫혔다. 아래 후속 처리
조건을 구현 단계 지시에 포함하는 것을 조건으로 APPROVE 를 권고한다.

---

## 2차 잔존 2건 — 해소 여부 표

| # | 2차 잔존 결함 | codex 판정 | 실행자 검증 후 최종 | 근거 |
|---|---|---|---|---|
| [A] | §3.1 O3 키 문구 raw 정정 | **해소** | **해소** | §3.1 목록 좌열 키 문구 4건을 raw 원문(`**권고**만 하며` 볼드 마커 포함 등)으로 정정하고 §5.1 O3(i)에 `grep -F` 고정 문자열 대조 명시. **실행자 grep -F 직접 실측 결과 4건 모두 삭제 전 매치**(77·79·81·83행) — 부재 검사가 삭제 후에만 부재로 바뀌는 유의미한 검사로 성립. 여는 괄호 `(` 포함 3행도 `-F` 리터럴로 정상 매치 |
| [B] | §3.5 사람 apply 명령의 `--no-backup` 정리 | **해소** | **해소** | §3.5 dry-run·실제 apply 명령 모두 `--no-gitignore --no-backup` 포함(204·210행) — §4 Phase 1 스크립트 교체 요구(`--no-backup --no-gitignore`)와 통일됨. "설정과 동일 의미의 플래그를 명령에도 명시" 자기 설명과도 정합 |

요약: **2건 전부 해소.**

## grep -F 실측 결과 (실행자 직접 실행)
| 키 문구 (raw) | CLAUDE.md 매치 | 판정 |
|---|---|---|
| `**권고**만 하며` | 77행 매치 (`1 MATCH`) | 삭제 전 존재 — 유의미 |
| `증거 수집자와 판정자 분리` | 79행 매치 (`2 MATCH`) | 삭제 전 존재 — 유의미 |
| `판정 기준으로 삼는다(도구 실패 시` | 81행 매치 (`3 MATCH`) | 삭제 전 존재 — 여는 괄호 `-F` 리터럴 정상 |
| `모든 에이전트:` | 83행 매치 (`4 MATCH`) | 삭제 전 존재 — 유의미 |

4건 모두 삭제 전 매치되므로, O3(i) 부재 검사는 삭제 후에만 부재로 바뀌는(공허 통과 아님)
검사로 성립한다. 2차 결함 1(키 문구 "권고만 하며" 볼드 마커 누락)이 완전 해소됐다.

---

## 결함 목록 (심각도순 — (A)계획 결함 / (B)구현 세부 분류)

### (A) 계획 문서로서의 반려 결함: **발견되지 않음.**

### (B)-1. `--no-gitignore`·`--no-backup` 실재 미검증 — Phase 1 첫 단계에서 확정
- 근거: 두 플래그의 실재는 [B-1] apply --help 캡처(Phase 1) 이전이라 미검증 상태다. 그러나
  계획이 §4 Phase 1 검증에 캡처 절차를, §3.5 에 미실재 폴백(플래그 제거 + toml 신뢰 +
  git diff 방어)을 명시하므로 통제된다.
- 처리: Phase 1 에서 `apply --help` 원문 캡처로 실재 확정(후속 처리 조건 1).

### (B)-2. O3 문구의 "삭제 대상 3건" 과 "키 문구 4건" 병기 — 후속 검증 기록으로 해소
- 근거: §5.1 O3 문구가 "삭제 대상 3건" 과 "키 문구 4건" 을 함께 쓴다(3개 삭제 그룹 / 4개 raw
  키 문자열이라는 뜻). §3.1 이 4건을 명시하므로 오독 소지는 낮으나, 검증 시 4개 문자열 전부의
  `grep -F` 부재 결과를 남기면 완결된다.
- 처리: O3 검증(impl-verifier/final-tester)에서 4개 raw 문자열 전부의 grep -F 부재 결과를
  증거로 기록(후속 처리 조건 2).

---

## plan-writer 확인 포인트 3건 답변

1. **§3.1 키 문구 4건 raw 정합 최종 확정 여부**: **최종 확정** (codex·실행자 grep -F 실측
   일치). 지정된 4개 `grep -F` 모두 삭제 전 현재 원문에서 매치되며, §5.1 O3(i)에 grep -F
   고정 문자열 대조가 명시돼 마크다운 마커를 정규식 메타로 오인할 위험도 차단됐다. 2차 결함
   1 완전 해소.

2. **[B] 플래그 실재 2단 유보의 수용성**: **(B) 구현·검증 단계 세부로 수용 가능** (codex·실행자
   일치). 승인 전 실물 검증이 필요한 (A) 결함이 아니다 — 근거: (i) 계획이 Phase 1 **첫 검증
   항목**으로 캡처를 배치해 구현 착수 직후 확정되고, (ii) 미실재 시 폴백(플래그 제거 + toml
   설정 신뢰 + 절차 4 git diff 방어)이 구체적이며, (iii) 실쓰기 안전은 플래그가 아니라 실행
   주체 분리(사람 한정)·toml `[gitignore]=false`·`[backup]=false` 로 이미 담보된다. 플래그는
   "이중 방어" 이지 유일 방어가 아니므로 실재 여부가 안전을 좌우하지 않는다. Phase 1 에서
   충분히 통제된다.

3. **O3(ii) 의미 대조 판단 경계**: **충분함 유지** (2차 충분 판정이 3차 개정으로 훼손되지
   않음). `.ruler/20-process.md`·`30-evidence.md`·`10-safety.md` 에 대응 절 제목·키워드가
   실재하고(실행자 대조 확인), 계획이 동일 문구 grep 이 아니라 절 제목·키워드 확인(기계) +
   impl-verifier 의미 판단으로 경계를 유지하며 "`.ruler/` 중복 문장 추가로 통과 금지" 가
   우회로를 막는다.

---

## 후속 처리 조건 (APPROVE 권고의 조건 — 구현·검증 단계 지시에 포함할 것)

1. **플래그 실재 확정**: Phase 1 에서 `npx @intellectronica/ruler@0.3.44 apply --help` 원문을
   캡처해 `--no-gitignore`·`--no-backup` 실재를 확정한다. 미실재 플래그가 있으면 §3.5·스크립트
   명령에서 제거하고 `[gitignore]=false`·`[backup]=false` + git diff 방어를 검증 증거로 남긴다.
   (implementer — Phase 1)
2. **O3 검증 기록**: O3 검증 시 삭제 그룹 3개가 아니라 raw 키 문자열 **4개 전부**의 `grep -F`
   부재 결과를 증거로 기록한다. (impl-verifier/final-tester)

---

## 실행자 직접 검증 요약 (도구 결과와 별개 수행)
| 확인 항목 | 결과 |
|---|---|
| §3.1 키 문구 4건 × CLAUDE.md `grep -F` 직접 실행 | 4건 전부 삭제 전 매치(77·79·81·83행, 각 `MATCH`) — [A] 완전 해소. 여는 괄호 포함 3행도 -F 리터럴 정상 |
| §3.1 목록 2·3행 raw 기재 + "raw 대조 결과 요약" 문단 | 원문 확인 — 4건 raw 형태·마커 유무 정확히 기재됨 |
| §5.1 O3(i) 에 `grep -F` 고정 문자열 대조 명시 | 원문 확인 — 축약형 grep 금지 사유까지 명기 |
| §3.5 apply 명령(dry-run·실제) `--no-gitignore --no-backup` | 원문 확인(204·210행) — 스크립트 요구와 통일 [B] 해소 |
| §3.1 우열 대응 의미 키워드 × `.ruler/` 3파일 | 20-process·30-evidence·10-safety 절 제목·키워드 전부 실재 — O3(ii) 기준표 유효 |
| 개정 이력·§7 "재점검 시 남는 미결 없음" | 확인 — 2차 결함 2건 반영 완료 표 기재 |

**실행자 검증 주석**
- codex 의 해소 판정 2건·(B) 결함 2건·확인 포인트 답변 3건은 원문·grep -F 실측 대조 결과 모두
  근거가 실재하며 명백한 오류가 발견되지 않았다. 결함 [A] 해소는 실행자·codex 가 동일 grep -F
  실측으로 교차 확인한 사항이다.
- 이 계획은 3회 점검을 거치며 (A)급 결함이 순차 해소됐고, 이번 3차에서 (A) 부재 + (B) 2건만
  남았다. (B) 는 모두 Phase 1 첫 단계에서 통제되는 성격이므로 조건부 APPROVE 권고가 타당하다
  (1차 테스트 PLAN 이 4차에서 같은 방식으로 조건부 APPROVE 통과한 선례와 정합).

---

## 부록 A — codex(gpt-5.5) 3차 재점검 최종 응답 원문 전문

> 아래는 외부 점검 도구 codex 의 3차 재점검 최종 응답 원문이다. `tokens used`: 40,561.
> (실행 로그 원본: 위 "점검 수행자" 절의 tool-results 경로. 로그에는 동일 응답이 스트리밍
> 특성상 2회 표시되나 내용은 동일하므로 1회만 옮긴다.)

```
판정: **APPROVE (조건부 — 후속 처리 조건 명시)**

3차 개정으로 2차 REVISE의 반려급 (A) 결함은 해소됐습니다. 남는 것은 `--no-gitignore`·
`--no-backup` 실재 확인처럼 Phase 1 구현·검증 첫 단계에서 자연히 확정될 (B) 세부입니다.
따라서 무한 반려할 사안은 아니고, 조건부 APPROVE가 가능합니다.

**2차 잔존 2건 해소 여부**

1. §3.1 O3 키 문구 raw 정정: **해소**
   직접 `grep -F` 실측 결과 4건 모두 현재 `CLAUDE.md`에서 매치됩니다.

77:  모으고 **권고**만 하며, ...
79:  진행하지 않는다(증거 수집자와 판정자 분리). ...
81:  판정 기준으로 삼는다(도구 실패 시 ...
83:- 모든 에이전트: §0 "위험 작업 목록"의 실쓰기 금지(드라이런까지).

계획서도 [PLAN_RULER_MIGRATION.md:125]에 `**권고**만 하며`, `증거 수집자와 판정자 분리`,
`판정 기준으로 삼는다(도구 실패 시`, `모든 에이전트:`를 raw 키 문구로 기재했고, [§5.1 O3:322]에
`grep -F` 고정 문자열 대조를 명시했습니다. 삭제 전 존재하므로 삭제 후 부재 검사가 유의미합니다.

2. §3.5 `--no-backup` 정리: **해소**
   §3.5 dry-run과 실제 apply 모두 `--no-gitignore --no-backup`을 포함합니다:
   [PLAN_RULER_MIGRATION.md:204], [PLAN_RULER_MIGRATION.md:210].
   §4 Phase 1도 `run-ruler-test.sh`의 기존 4곳 `--backup --gitignore`를
   `--no-backup --no-gitignore`로 교체하라고 명시합니다: [PLAN_RULER_MIGRATION.md:283].

**신규·잔존 결함 목록**

- [Low] (B) `--no-gitignore`·`--no-backup` 실재 미검증은 남아 있음. 다만 계획서가 Phase 1에서
  `apply --help` 캡처 후 미실재 시 플래그 제거 + `ruler.toml` 신뢰 + `git diff` 방어로
  폴백하라고 명시하므로 반려 사유가 아닙니다.
- [Low] (B) O3 문구가 "삭제 대상 3건"과 "키 문구 4건"을 함께 씁니다. 문맥상 3개 삭제 그룹/4개
  raw 키 문자열이라 해석 가능하고 §3.1이 4건을 명시하므로 반려 사유는 아닙니다. 후속 검증에서는
  4개 raw 문자열 전부 `grep -F`로 기록하면 됩니다.

(A) 계획 문서 결함은 발견하지 못했습니다.

**plan-writer 확인 포인트 3건**

1. §3.1 키 문구 4건 raw 정합: **최종 확정 가능**. 지정된 4개 `grep -F` 모두 삭제 전 현재
   원문에서 매치됩니다.
2. 플래그 실재의 2단 유보 수용성: **(B) 구현·검증 단계 세부로 수용 가능**. 승인 전 실물 검증이
   필요한 (A) 결함은 아닙니다. Phase 1 첫 검증 항목으로 충분히 통제됩니다.
3. O3(ii) 의미 대조 판단 경계: **충분함 유지**. `.ruler/20-process.md`, `30-evidence.md`,
   `10-safety.md`에 대응 절 제목·키워드가 존재하고, 계획도 동일 문구 grep이 아니라 절 제목·
   키워드 확인 + impl-verifier 의미 판단으로 경계를 유지합니다.

**후속 처리 조건**

- Phase 1에서 `npx @intellectronica/ruler@0.3.44 apply --help` 원문을 캡처해 `--no-gitignore`·
  `--no-backup` 실재를 확정할 것.
- 미실재 플래그가 있으면 §3.5와 스크립트 명령에서 해당 플래그를 제거하고, `[gitignore]=false`·
  `[backup]=false` 및 `git diff` 방어를 검증 증거로 남길 것.
- O3 검증 시 삭제 그룹 3개가 아니라 raw 키 문자열 4개 전부의 `grep -F` 부재 결과를 기록할 것.

tokens used
40,561
```

---

## 완료 안내
정본 이관 계획 3차 재점검의 증거 수집·권고(**APPROVE 조건부**)를 마쳤다. **최종 판정은
gate-judge 가 재판정으로 확정해야 한다** — APPROVE 확정 시 후속 처리 조건 2건을 구현(3단계)
지시에 포함할 것을 함께 판정 기록에 남기기를 권고한다. gate-judge 호출 예: "gate-judge 로
PLAN_RULER_MIGRATION_REVIEW(3차) 판정해줘". JUDGE 의 APPROVE 확정 전에는 implementer 를
호출하지 않는다.
